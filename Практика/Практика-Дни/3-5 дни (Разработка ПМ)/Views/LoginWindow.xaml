<Window x:Class="WpfApp1.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Технолог - Вход в систему" Height="647" Width="480"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <!-- Цвета -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2196F3"/>
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#1976D2"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#4CAF50"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="#F44336"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#757575"/>

        <!-- Тени -->
        <DropShadowEffect x:Key="WindowShadow" BlurRadius="25" Opacity="0.3" ShadowDepth="0"/>
        <DropShadowEffect x:Key="ButtonShadow" BlurRadius="6" Opacity="0.15" ShadowDepth="1"/>

        <!-- Стили -->
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#424242"/>
            <Setter Property="Margin" Value="0,5,0,5"/>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Фоновый градиент -->
        <Rectangle Fill="#2196F3" Opacity="0.15"/>
        <Rectangle Fill="White" Opacity="0.95"/>

        <!-- Основная карточка -->
        <Border Background="White" CornerRadius="16" Effect="{StaticResource WindowShadow}" 
                Width="440" Height="608" HorizontalAlignment="Center" VerticalAlignment="Center">

            <Grid Margin="30,25,30,25">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Логотип и заголовок -->
                <StackPanel Margin="0,0,0,25" HorizontalAlignment="Center">
                    <Border Width="80" Height="80" CornerRadius="40" Background="#E3F2FD" 
                            HorizontalAlignment="Center" Effect="{StaticResource ButtonShadow}">
                        <TextBlock Text="🔬" FontSize="48" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Модуль Технолога" FontSize="24" FontWeight="Bold" 
                               Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,15,0,5"/>
                    <TextBlock Text="Система управления производством" FontSize="12" 
                               Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                </StackPanel>

                <!-- Поля ввода -->
                <TextBlock Grid.Row="1" Text="Логин" Style="{StaticResource LabelStyle}"/>
                <TextBox Grid.Row="2" x:Name="txtUsername"/>

                <TextBlock Grid.Row="3" Text="Пароль" Style="{StaticResource LabelStyle}"/>
                <PasswordBox Grid.Row="4" x:Name="txtPassword"/>

                <!-- CAPTCHA -->
                <TextBlock Grid.Row="5" Text="Код подтверждения" Style="{StaticResource LabelStyle}"/>
                <StackPanel Grid.Row="6" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBox x:Name="txtCaptcha" Width="120" Height="42" FontSize="16" 
                             FontWeight="Bold" TextAlignment="Center" MaxLength="4"/>
                    <Border Margin="12,0,0,0" CornerRadius="8" BorderBrush="#E0E0E0" 
                            BorderThickness="1" Background="#F5F5F5" Width="130" Height="42">
                        <Image x:Name="imgCaptcha" Stretch="Fill" Margin="5"/>
                    </Border>
                    <Button x:Name="btnRefreshCaptcha" Width="44" Height="42" Margin="10,0,0,0" 
                            Click="RefreshCaptcha_Click" Background="#607D8B" Foreground="White"
                            Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                    <TextBlock Text="🔄" FontSize="20" HorizontalAlignment="Center" 
                                               VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>

                <!-- Ошибка -->
                <TextBlock x:Name="lblError" Grid.Row="7" Foreground="{StaticResource ErrorBrush}" 
                           FontSize="12" HorizontalAlignment="Center" Margin="0,5,0,5"
                           TextWrapping="Wrap" TextAlignment="Center"/>

                <!-- Кнопки -->
                <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button x:Name="btnLogin" Content="ВОЙТИ" Click="Login_Click" 
                            Width="130" Height="44" Cursor="Hand"
                            Background="{StaticResource PrimaryBrush}" Foreground="White" 
                            FontSize="14" FontWeight="SemiBold" BorderThickness="0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button x:Name="btnRegister" Content="РЕГИСТРАЦИЯ" Click="Register_Click" 
                            Width="130" Height="44" Margin="15,0,0,0" Cursor="Hand"
                            Background="Transparent" Foreground="{StaticResource PrimaryBrush}" 
                            FontSize="14" FontWeight="SemiBold" BorderBrush="{StaticResource PrimaryBrush}" 
                            BorderThickness="1">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>

                <!-- Нижняя информация -->
            </Grid>
        </Border>
    </Grid>
</Window>