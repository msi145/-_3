<UserControl x:Class="WpfApp1.Views.BatchMonitoringView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F5F5F5">

    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2196F3"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#4CAF50"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#FF9800"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#F44336"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#E0E0E0"/>

        <DropShadowEffect x:Key="CardShadow" BlurRadius="10" Opacity="0.08" ShadowDepth="2"/>

        <Style TargetType="DataGrid">
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#FAFAFA"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
            <Setter Property="RowHeight" Value="42"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" 
                                Background="{TemplateBinding Background}"
                                BorderThickness="0,0,0,2"
                                BorderBrush="Transparent"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="ContentSite"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"
                                              ContentSource="Header"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <Border Grid.Row="0" Background="White" CornerRadius="10" Padding="20" Margin="0,0,0,15" Effect="{StaticResource CardShadow}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📊" FontSize="28"/>
                        <TextBlock Text="Мониторинг партий" FontSize="22" FontWeight="Bold" 
                                   Foreground="#2C3E50" Margin="12,0,0,0"/>
                    </StackPanel>
                    <TextBlock Text="Отслеживание хода выполнения производственных партий в реальном времени" 
                               FontSize="13" Foreground="#757575" Margin="40,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Выбор партии -->
            <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="15" Margin="0,0,0,15" Effect="{StaticResource CardShadow}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Выберите партию:" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold" Margin="0,0,15,0"/>
                    <ComboBox x:Name="cmbBatches" Grid.Column="1" Height="38" FontSize="13"
                              VerticalContentAlignment="Center" SelectionChanged="Batch_SelectionChanged"/>

                    <Button x:Name="btnRefresh" Grid.Column="2" Content="🔄 Обновить" Click="Refresh_Click" 
                            Width="100" Height="38" Margin="15,0,0,0"
                            Background="#2196F3" Foreground="White" FontSize="13" FontWeight="SemiBold"/>
                </Grid>
            </Border>

            <!-- Информационная панель -->
            <Border Grid.Row="2" Background="White" CornerRadius="10" Padding="15" Margin="0,0,0,15" Effect="{StaticResource CardShadow}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="#E3F2FD" CornerRadius="8" Padding="12" Margin="5">
                        <StackPanel>
                            <TextBlock Text="📦" FontSize="22"/>
                            <TextBlock x:Name="tbBatchNumber" Text="Не выбрана" FontSize="14" FontWeight="SemiBold" Foreground="#1976D2" Margin="0,5,0,0"/>
                            <TextBlock Text="Номер партии" FontSize="11" Foreground="#757575"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Background="#E8F5E9" CornerRadius="8" Padding="12" Margin="5">
                        <StackPanel>
                            <TextBlock Text="⚙️" FontSize="22"/>
                            <TextBlock x:Name="tbStatus" Text="-" FontSize="14" FontWeight="SemiBold" Foreground="#388E3C" Margin="0,5,0,0"/>
                            <TextBlock Text="Статус" FontSize="11" Foreground="#757575"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Background="#FFF3E0" CornerRadius="8" Padding="12" Margin="5">
                        <StackPanel>
                            <TextBlock Text="📅" FontSize="22"/>
                            <TextBlock x:Name="tbStartDate" Text="-" FontSize="14" FontWeight="SemiBold" Foreground="#F57C00" Margin="0,5,0,0"/>
                            <TextBlock Text="Дата начала" FontSize="11" Foreground="#757575"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="3" Background="#F3E5F5" CornerRadius="8" Padding="12" Margin="5">
                        <StackPanel>
                            <TextBlock Text="👤" FontSize="22"/>
                            <TextBlock x:Name="tbResponsible" Text="-" FontSize="14" FontWeight="SemiBold" Foreground="#7B1FA2" Margin="0,5,0,0"/>
                            <TextBlock Text="Ответственный" FontSize="11" Foreground="#757575"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- Таблицы -->
            <TabControl Grid.Row="3" SelectionChanged="TabControl_SelectionChanged">
                <TabItem Header="📋 Шаги производства">
                    <DataGrid x:Name="dgSteps" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,10,0,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="№" Binding="{Binding StepNumber}" Width="60"/>
                            <DataGridTextColumn Header="Название шага" Binding="{Binding StepName}" Width="220"/>
                            <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="120"/>
                            <DataGridTextColumn Header="Начало" Binding="{Binding StartedAt, StringFormat='dd.MM.yyyy HH:mm'}" Width="150"/>
                            <DataGridTextColumn Header="Окончание" Binding="{Binding FinishedAt, StringFormat='dd.MM.yyyy HH:mm'}" Width="150"/>
                            <DataGridTextColumn Header="Значение" Binding="{Binding ActualParamValue}" Width="100"/>
                            <DataGridTemplateColumn Header="Действие" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="✅" Width="30" Height="28" 
                                                Click="CompleteStep_Click" Tag="{Binding}"
                                                Background="#4CAF50" Foreground="White" FontSize="12"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>

                <TabItem Header="⚠️ Отклонения">
                    <DataGrid x:Name="dgDeviations" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,10,0,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Тип" Binding="{Binding DeviationType}" Width="140"/>
                            <DataGridTextColumn Header="Важность" Binding="{Binding Severity}" Width="100"/>
                            <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="350"/>
                            <DataGridTextColumn Header="Статус" Binding="{Binding ResolutionStatus}" Width="120"/>
                            <DataGridTextColumn Header="Зарегистрировано" Binding="{Binding RegisteredAt, StringFormat='dd.MM.yyyy HH:mm'}" Width="160"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>

                <TabItem Header="📦 Использованное сырье">
                    <DataGrid x:Name="dgMaterials" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,10,0,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Материал" Binding="{Binding MaterialName}" Width="250"/>
                            <DataGridTextColumn Header="План (кг)" Binding="{Binding PlannedQty}" Width="120"/>
                            <DataGridTextColumn Header="Факт (кг)" Binding="{Binding ActualQty}" Width="120"/>
                            <DataGridTextColumn Header="Отклонение" Binding="{Binding Deviation}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>
            </TabControl>

            <!-- Кнопки действий -->
            <Border Grid.Row="4" Background="White" CornerRadius="10" Padding="15" Margin="0,15,0,0" Effect="{StaticResource CardShadow}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="btnCompleteBatch" Content="✅ Завершить партию" Click="CompleteBatch_Click" 
                            Width="160" Height="40" Background="#4CAF50" Foreground="White" FontSize="13" FontWeight="SemiBold" Margin="0,0,10,0"/>
                    <Button x:Name="btnAddDeviation" Content="⚠️ Зарегистрировать отклонение" Click="AddDeviation_Click" 
                            Width="220" Height="40" Background="#FF9800" Foreground="White" FontSize="13" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>